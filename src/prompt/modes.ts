// 4모드 정의 — 장르가 아니라 "독자 목적"으로 나눈다.
// 새 모드가 생겨도 여기 조각 하나만 추가하면 된다. (안전 규칙은 skeleton에 단일화)

export type Mode = '적용' | '검증' | '논증' | '소화'

export const MODES: Mode[] = ['적용', '검증', '논증', '소화']

export const MODE_META: Record<Mode, { label: string; desc: string }> = {
  적용: { label: '적용 (Transfer)', desc: '배워서 써먹는 글 · 기술/개념/튜토리얼/방법론/레시피' },
  검증: { label: '검증 (Scrutiny)', desc: '사실을 전달하는 글 · 뉴스/보도/발표' },
  논증: { label: '논증 (Argument)', desc: '설득하려는 글 · 칼럼/사설/주장' },
  소화: { label: '소화 (Digest)', desc: '읽고 음미하는 글 · 에세이/교양/서사' },
}

// [모드 공식] 자리에 끼워지는 조각들
// 모든 모드는 "근거 문단(anchor)을 먼저 고르고 → 그 문단으로 답하는 질문"의 순서를 따른다.
export const MODE_FORMULAS: Record<Mode, string> = {
  적용: `[적용 모드 공식]
1. 핵심 개념 하나와, 그 개념이 성립하는 "조건"이 적힌 문단을 원문에서 고른다 → anchor.
2. 그 문단을 읽으면 답할 수 있는 질문을 만든다.
   (예: 이 개념이 성립하려면 원문은 어떤 조건을 전제하는가?)`,

  검증: `[검증 모드 공식]
1. 핵심 "사실 주장" 하나와, 그 주장의 근거·주체·조건이 적힌 문단을 원문에서 고른다 → anchor.
2. 그 문단을 읽으면 답할 수 있는 질문을 만든다.
   (예: 누가, 무엇을, 어떤 근거로 판단·주장했는가?)
   원문에 적히지 않은 "사회적 영향·함의·전망"은 절대 묻지 않는다.`,

  논증: `[논증 모드 공식]
1. 글쓴이의 핵심 "주장"과 그 "근거"가 적힌 문단을 원문에서 고른다 → anchor.
2. 그 문단을 읽으면 답할 수 있는 질문을 만든다.
   (예: 글쓴이는 무엇을 어떤 근거로 주장하는가?)`,

  소화: `[소화 모드 공식]
1. 핵심 문단 하나를 원문에서 고른다 → anchor.
2. 그 문단을 독자 "자신의 말"로 재구성하게 하는 질문을 만든다.`,
}
